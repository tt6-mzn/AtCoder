#include <bits/stdc++.h>
using namespace std;
#define rep(i, n) for (int i = 0; i < (int)(n); i++)

int main() {
    int h, w;
    cin >> h >> w;
    vector<string> c(h);
    rep(i, h) {
        cin >> c[i];
    }
    rep(i, h) {
        cout << c[i] << endl;
        cout << c[i] << endl;
    }
}