#include <bits/stdc++.h>
using namespace std;
#define rep(i, n) for(int i = 0; i < (int)(n); i++)

int main() {
    string a, b;
    cin >> a >> b;
    if(a.size() > b.size()) cout << a << endl;
    else cout << b << endl;
}
